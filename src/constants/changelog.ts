/**
 * 更新日志数据源。
 *
 * 新增版本时，在 {@link CHANGELOG_DATA} 数组**顶部**插入一条记录（最新版本在最前）。
 * 展示格式规范（由 `src/app/changelog.tsx` 统一渲染，不要写死在数据里）：
 *
 *   v x.x.x：
 *   - 更新内容1
 *   - 更新内容2
 *
 * 即 `version` 字段渲染成 `v <version>：` 标题行，`items` 的每一项渲染成 `- xxx`。
 */

export type ChangelogEntry = {
  /** 版本号，只写数字部分，例如 '1.0.0'；标题的 `v ` 前缀与结尾冒号由页面渲染。 */
  version: string;
  /** 该版本的更新要点，按展示顺序排列。 */
  items: string[];
};

export const CHANGELOG_DATA: ChangelogEntry[] = [
  {
    version: '1.1.2',
    items: ['修复冷启动后「导出数据库」弹窗中本机快照份数始终显示为 0 的问题'],
  },
  {
    version: '1.1.1',
    items: ['关于页新增「获取最新发布」入口，可直达 GitHub Release 页面'],
  },
  {
    version: '1.1.0',
    items: [
      '新增「更新日志」页面，可在「关于」页查看历史版本更新',
      '修复安卓桌面图标被系统圆角裁切、边缘显示不完整的问题',
      '修复启动时出现灰白占位画面的问题，启动屏与应用主色调保持一致',
      '修复添加商品弹窗被软键盘遮挡、产品下拉列表无法上下滑动的问题',
    ],
  },
  {
    version: '1.0.0',
    items: [
      '正式发布 1.0.0 版本',
      '支持双列商品展示与低库存自动升序预警',
      '支持摄像头连续条形码扫描出库、入库与查询',
      '支持多维度日志流水检索与安全数据库导入导出',
    ],
  },
];
